<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neko.mapper.BorrowDetailMapper">

    <insert id="insertBatch"> insert into borrow_details (name, borrow_record_id, book_id, number,
        amount, image) values <foreach collection="borrowDetailList" item="bd" separator=","> (
        #{bd.name}, #{bd.borrowRecordId}, #{bd.bookId}, #{bd.number}, #{bd.amount}, #{bd.image} ) </foreach>
    </insert>

</mapper>